on:
  issues:
    types: [labeled]

jobs:
  automate_invite:
    runs-on: ubuntu-latest
    steps:
      - name: Dear Santa Action
        uses: imjohnbo/dear-santa-action@v1.0.5
        with:
          label: wishlist
